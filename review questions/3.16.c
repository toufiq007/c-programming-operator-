#include <stdio.h>
int main()
{
    int x = 10;
    // here we first declare and initialize the x variable but in if block we change the value of x and assign 20 in x so if condition is true and if block is execute

    if(x=20){
        printf("TRUE\n");
    }
    else{
        printf("FALSE\n");
    }
    return 0;
}
