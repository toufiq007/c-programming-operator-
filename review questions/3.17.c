#include <stdio.h>
int main()
{
    int m = 1, n = 5;
    // first one
    if (m==1 & n!=0){
        printf("OK\n");
    }

    // second one
    if (m < 5){
        printf("JUMP\n");
    }

    return 0;
}