#include <stdio.h>
int main()
{
    int x = 10;
    float y = 4.25;

    //x= y%x; // here is the problem we cannot find modulus for floating devision number..The two number must be an Integer to find the modulus





    return 0;
}